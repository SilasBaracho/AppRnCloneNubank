import styled from 'styled-components/native';

import VectorIcons from 'react-native-vector-icons/Feather';

export const Container = styled.View`
  width: 60px;
  height: 60px;
  border-radius: 30px;
  justify-content: center;
  align-items: center;
  background-color: rgba(255, 255, 255, 0.2);
`;

export const Icon = styled(VectorIcons)``;
